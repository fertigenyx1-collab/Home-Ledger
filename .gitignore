# Built application files
*.apk
*.aar
*.aab

# Keystore files, certificates, & signing credentials (NEVER commit these!)
*.jks
*.keystore
*.key
*.pepk
*.p12
*.bks
keystore.properties
debug.keystore
debug.keystore.base64
my-upload-key.jks
upload-keystore.jks

# Environment variables & API keys
.env
.env.local
.env.*.local
local.properties

# Gradle & Kotlin caches
.gradle
.kotlin
/build
/*/build
/captures
.externalNativeBuild
.cxx

# Gradle Wrapper JAR (generated locally, causes validation issues in CI)
gradle/wrapper/gradle-wrapper.jar

# IDE & Editor files
*.iml
.idea
.DS_Store

# Google Services (if containing private client IDs/certs)
# google-services.json

# Release directory
release/
build-artifacts/
